# 牧客智语前端 UI 与交互动画实施规范

> 文档版本：Demo UI v2.0  
> 文档日期：2026-09-08  
> 适用范围：当前 `web-prototype` 可交互演示版本  
> 主要读者：负责复刻、扩展或接入后端的 AI 编程代理  
> 文档目标：仅凭本文即可理解并重建当前前端的页面、组件、状态、动画和完整演示流程，不依赖任何设计图片。

## 0. v2.0 冻结规则（优先于下方历史章节）

本节记录2026-09-08确认后的最新规则。下方章节若仍出现“断奶后腹泻”“可信度标签”“默认突出第一方向”“混合结果”或“只显示最新一轮响应”等旧描述，以本节为准。

### 0.1 固定 Demo 与真实联调必须分离

- 首页提供独立入口“体验完整问诊 Demo”。
- 固定 Demo 使用前端脚本数据，不调用 Coze API，用于确认完整流程、文案层级、卡片和动画。
- 普通示例问题和输入框进入真实联调，不得在真实结果区插入固定 Demo 数据。
- 固定案例为：`3日龄哺乳仔猪，10头里有5头拉稀，已经两天了`。
- 不得重复询问已经提供的日龄、数量和病程。当前固定追问为饮水精神状态和粪便性状。

### 0.2 真实对话时间线

真实联调不能只保存最新 `WorkflowResponse`。每轮必须依次保留：

```text
用户原始描述
→ AI结构化追问卡片
→ 已回答追问卡片（原位只读保留）
→ 正在分析的过渡状态
→ AI下一轮回复
→ 用户选择的解决方向（聊天气泡）
→ 正在匹配产品的过渡状态
→ 产品、管理或无匹配结果
```

实现规则：

1. `response_type=question` 时只渲染 `questions[]`，不得再把包含同样问题的 `response` 重复显示一遍。
2. 单选答案使用用户可见 `label`；选择“其他”时显示 `other_text`，不得显示内部值。
3. 用户提交本轮全部必答题后，交互卡片在原时间线位置变为只读“本轮信息已补充”卡片；每一项保留问题和最终答案，不再另加用户聊天气泡。
4. 用户选择方向后显示 `我选择：方向名称`，不能只传隐藏 ID。
5. API 请求期间保留用户消息，并立即显示分析过渡动画。
6. 新响应到达后追加展示，不删除上一轮已回答卡片；用户最初的自由文本描述仍使用用户聊天气泡。
7. `conversation_context` 仍传上一轮完整结构化响应；UI时间线与工作流状态是两个不同概念。

诊断结果顶部先从 `case_data` 提取非空字段，使用中文标签展示“已确认病例信息”，至少覆盖日龄、生长阶段、主要症状、持续时间、粪便颜色、粪便性状、发病范围、死亡情况、严重程度、体温、饮水及采食状态；不得直接暴露 `age`、`stage`、`feces_color` 等内部字段名。诊断标题正常一次性出现，不使用逐字打印动画。

病例卡片已经承载事实依据后，不再在诊断正文下重复渲染 `diagnosis.evidence[]` 列表，尤其禁止出现 `stage:`、`symptoms:`、`feeding_status:` 等英文键名。`evidence[]` 仍保留在接口数据中用于追溯，只是不直接显示给用户。

已回答追问卡片采用约 `65% / 35%` 的问题与答案双栏布局。问题保持左对齐，但在当前行内垂直居中；答案在右栏同时水平居中和垂直居中，答案换行时每行也居中。卡片标题使用深蓝灰，不使用绿色。

### 0.3 节点5诊断展示

形成临床高可能疾病后依次展示：疾病名称、发生机制、与病例相符的关键依据、当前管理与排查建议、风险提示。不得展示内部候选、排序、RAG过程或百分比置信度。

“当前管理与排查建议”使用一个浅绿色连续模块：主标题字号约15px，辅助说明约11px，正文约12px；所有建议使用同一组圆点列表，不使用横线拆成上下两个区域。

### 0.4 节点6方向展示

方向前先展示总体管理建议。方向标题区文案应自然承接：`完成基础管理与排查后，可以选择一个方向，继续查看针对当前问题的改善方式。`

每个方向卡片至少展示：

- `name`：方向名称；
- `target_problem`：针对的问题；
- `mechanism`：作用方式；
- `expected_improvement`：预期改善。

不得展示内部推荐层级。1—3张卡片分别使用同等视觉权重的浅蓝、浅绿、浅暖灰；只有用户点击后才使用品牌蓝边框表达选中。

### 0.5 节点7产品与购买渠道

产品名称、厂家和推荐原因优先展示，其余信息通过“查看完整产品资料”渐进展开。单次展示1—4个产品，当前产品目标通常为2—3个，但不得为了凑数跨方向或虚构产品。

前端产品视图模型必须预留：

```ts
interface PurchaseChannel {
  official_phone: string | null;
  official_website: string | null;
  purchase_method: string | null;
  channel_status: string | null;
}

interface ProductViewModel {
  product_id: string;
  product_name: string;
  manufacturer: string | null;
  product_category: string | null;
  ingredients: string | null;
  positioning: string | null;
  applicable_timing: string | null;
  recommendation_reason: string;
  usage: string | null;
  precautions: string | null;
  purchase_channel: PurchaseChannel | null;
  source_id: string;
}
```

渠道展示规则：

- 有 `official_website`：显示可点击的“访问厂家官网”，新窗口打开，并使用 `noopener,noreferrer`。
- 有 `official_phone`：显示厂家官方电话，但不得自动拨号或暗示平台背书。
- 有购买方式或渠道状态：原样展示经核验的资料。
- 字段缺失：显示“产品资料暂无相关信息”，不得由前端猜测、拼接或临时搜索。
- `source_id` 必须存在；页面无需默认展示内部ID，但应可用于审计和排查。
- `no_match` 时产品数组必须为空，并显示没有可靠匹配的说明。

### 0.6 结果类型边界

- `direction_selection`：仅展示方向，选择前 `selected_direction=null`。
- `product`：只展示用户已选产品型方向下的产品。
- `management`：展示管理建议，产品数组必须为空。
- `no_match`：明确无可靠产品，禁止用其他方向产品补位。
- `emergency`：停止普通方向和产品流程。
- 不再使用旧版 `mixed`。

## 1. 产品与 Demo 定位

牧客智语是面向养猪用户的 AI 辅助问诊与方案推荐产品。当前版本是用于验证 UI、交互节奏、信息层级和完整问诊路径的前端 Demo，不是真实诊断系统。

本 Demo 必须做到：

1. 能完整演示一次正常猪病问诊流程。
2. 用户能够真实点击、输入、选择和推进流程。
3. 使用本地脚本数据，不调用大模型、数据库、短信、微信或产品检索 API。
4. 页面结构必须能够在未来无大规模重写的前提下替换为真实 API 数据。
5. 所有医学结论都使用辅助判断口吻，不能表现为确诊。
6. 所有产品内容必须标注为 Demo 示例，不能伪装成知识库正式检索结果。

## 2. 当前技术边界

### 2.1 技术栈

- React 19。
- TypeScript。
- Vite。
- Motion for React，负责进入、退出、顺序显现和状态切换动画。
- Radix UI Icons，负责统一的线性图标。
- Noto Sans SC Variable，负责中文字体。
- 项目自带手机运行框架，支持 iPhone 与 Pixel 10 两种设备预览。

### 2.2 Demo 不实现的能力

- 不执行真实微信授权。
- 不发送真实短信验证码。
- 不保存真实用户资料或历史对话。
- 不上传真实图片和语音。
- 不调用真实 AI 模型。
- 不执行 RAG 检索。
- 不从正式产品库返回产品。
- 不提供医疗确诊或处方。

点击这些能力时，可以展示合理的演示反馈，但不得暗示后台操作已经真实完成。

## 3. 设计原则

### 3.1 聊天优先

页面的绝对主角是用户问题、追问、辅助诊断和方案内容。不要增加资讯流、功能宫格、运营卡片或复杂导航。

### 3.2 专业但不冰冷

视觉采用安静、可信、克制的医疗问答风格。大面积使用雾白和浅蓝，不使用高饱和绿色，不使用夸张渐变，不使用大面积红色。

### 3.3 连续阅读

AI 回复使用连续的编辑式内容区，不把每一句话拆成独立聊天气泡，也不把结果切成大量互不关联的卡片。

### 3.4 渐进披露

每个阶段只展示用户当前需要处理的信息。流程顺序必须为：

```text
用户提问
→ 分析过程
→ 关键追问
→ 用户完成全部追问
→ 二次分析
→ 辅助诊断
→ 用户主动查看解决方向
→ 用户选择方向
→ 产品与管理方案
→ 流程完成
```

不得在用户尚未完成当前任务时自动跳转到下一阶段。

### 3.5 禁止推断用户未提供的信息

前端不得为了让演示顺畅而自动补齐用户答案。例如，用户只选择了日龄和粪便状态，就不能自动补充“仍能少量饮水”。

## 4. 设计令牌

### 4.1 颜色

| 令牌 | 色值 | 用途 |
| --- | --- | --- |
| `background` | `#F7F9FA` | 应用背景 |
| `surface` | `#FFFFFF` | 输入框、内容块、抽屉和卡片 |
| `surface-soft` | `#F1F5F7` | 搜索框、弱提示和浅色选中背景 |
| `text-primary` | `#202B33` | 标题与正文主文字 |
| `text-secondary` | `#667681` | 辅助说明 |
| `primary` | `#216A8A` | 主按钮、品牌图标、选中态 |
| `primary-bright` | `#087FB4` | 发送按钮和少量强调 |
| `border` | `#DDE6EA` | 默认边框与分隔线 |
| `warning-text` | `#B75D38` | 风险提示文字 |
| `warning-border` | `#F0CFC1` | 风险提示边框 |
| `warning-background` | `#FFF7F3` | 风险提示背景 |
| `danger` | `#BC5F57` | 删除、退出等危险操作 |

颜色限制：

- 红橙色只用于真正的风险提示或危险操作。
- 主流程不得使用绿色表达“正确诊断”。
- AI 辅助诊断不能通过颜色营造“已经确诊”的感觉。

### 4.2 字体

```css
font-family:
  "Noto Sans SC Variable",
  "Source Han Sans SC",
  "PingFang SC",
  "Microsoft YaHei",
  system-ui,
  sans-serif;
```

| 层级 | 字号 | 字重 | 行高 |
| --- | ---: | ---: | ---: |
| 登录页主标题 | 26px | 650–700 | 1.3 |
| 首页欢迎标题 | 24px | 680 | 1.3 |
| 诊断结果标题 | 18–19px | 680 | 1.55–1.62 |
| 区块标题 | 16–17px | 650 | 1.4 |
| 常规正文 | 13–14px | 400 | 1.65–1.8 |
| 按钮文字 | 11–14px | 500–650 | 1.4 |
| 标签和辅助信息 | 9–11px | 400–600 | 1.4 |

### 4.3 间距与圆角

- 页面左右主边距：手机端 17–22px。
- 基础间距单位：4px。
- 常用间距：8、10、12、14、16、18、20、24px。
- 主卡片圆角：15–18px。
- 输入框圆角：18–20px。
- 选项按钮圆角：9–11px。
- 胶囊标签圆角：999px。
- 所有主要触控区域视觉尺寸可以小于 44px，但实际点击区域应尽可能达到 44 × 44px。

### 4.4 阴影

阴影必须轻，不能形成电商卡片感。

```css
--shadow-soft: 0 6px 18px rgba(39, 74, 91, 0.06);
--shadow-card: 0 8px 25px rgba(39, 74, 91, 0.06);
--shadow-primary: 0 7px 18px rgba(33, 106, 138, 0.17);
```

## 5. 全局应用框架

### 5.1 手机运行容器

应用内容运行在项目已有手机容器中。不得删除或重写以下能力：

- iPhone 与 Pixel 10 设备切换。
- 系统状态栏。
- iOS Home Indicator。
- Android 导航栏和摄像头开孔。
- 模拟软键盘。
- 手机内容滚动和拖动抑制。
- 安全区域计算。

### 5.2 层级结构

```text
PhoneFrame
└─ KeyboardProvider
   ├─ StatusBar
   ├─ PrototypeShell
   │  ├─ TopBar
   │  ├─ ScrollableContent
   │  ├─ Composer
   │  ├─ HistoryDrawer
   │  ├─ DrawerScrim
   │  ├─ BottomSheet
   │  └─ Toast
   ├─ HomeIndicator
   └─ KeyboardDock
```

固定顶部栏、底部输入框、抽屉和弹层不能放入正文滚动容器。

## 6. 顶部栏

### 6.1 布局

- 定位：固定在应用视口顶部安全区以下。
- iPhone 顶部位置：54px。
- Android 顶部位置：64px。
- 高度：62px。
- 左右内边距：17px。
- 网格列：`54px 1fr 54px`。
- 背景：`#F7F9FA`。
- 底边框：1px `#DDE6EA`。

### 6.2 内容

- 左侧：首页和聊天页显示病历本图标，点击打开历史抽屉。
- 中间：主标题“牧客智语”。
- 中间副标题：“AI 建议仅供参考”，字号 10px，低对比度。
- 右侧：首页和聊天页显示新建对话图标。
- 设置页和个人信息页右侧显示关闭按钮。

图标必须来自统一线性图标库，不使用 Emoji 或文本字符模拟。

## 7. 登录流程

### 7.1 微信登录页

从上到下包含：

1. 圆形猪医生头像。
2. 品牌名“牧客智语”。
3. 主标题“欢迎回来”。
4. 说明“登录后，继续你的养殖问答”。
5. 主按钮“微信一键登录”。
6. 次按钮“使用手机号登录”。
7. 协议勾选行。

交互规则：

- 未勾选协议时点击登录，不进入首页。
- 页面内显示 Toast：“请先阅读并同意用户协议与隐私政策”。
- Demo 中点击微信登录直接进入首页，但不得展示“授权成功”等虚假后台结果。

### 7.2 手机号登录页

包含返回按钮、头像、品牌名、手机号输入、验证码输入、获取验证码、登录按钮和返回微信登录入口。

状态规则：

- 手机号或验证码任一为空时，登录按钮禁用。
- 输入框必须使用项目的键盘感知组件。
- 页面跳转前必须主动收起模拟键盘。

## 8. 首页

### 8.1 欢迎区域

- 顶部留白约 88px；矮屏降为约 45px。
- 头像容器 84 × 84px，白色背景，圆形边框和轻阴影。
- 标题：“你好，我是牧客智语”。
- Slogan：“让每一次养殖判断，都更有依据”。

### 8.2 示例问题

以平铺行显示，不使用独立大卡片：

1. 仔猪拉稀两天了怎么办？
2. 猪咳嗽、喘气是什么原因？
3. 母猪突然不吃料

点击任一示例问题等同于发送该问题，并进入聊天流程。

### 8.3 底部输入框

- 固定在底部安全区之上。
- 主容器为白色，圆角 18–20px，浅边框和轻阴影。
- 支持文本、多行自动增长、添加、拍照、语音和发送。
- 首页占位文案：“说说猪怎么了，或者你想了解什么”。
- 文本为空时，发送按钮禁用。
- 键盘出现时，整个输入区必须随键盘向上移动。

## 9. 完整问诊 Demo 状态机

### 9.1 状态定义

```ts
type DemoPhase =
  | "analyzing"
  | "followup"
  | "diagnosing"
  | "diagnosis"
  | "directions"
  | "products";
```

### 9.2 状态转换

```text
send(message)
  → analyzing

analyzing.stage 0
  → 780ms → stage 1
  → 780ms → stage 2
  → 780ms → stage 3
  → 360ms → followup

followup
  → 两题均有效 → 用户点击“完成并继续” → diagnosing

diagnosing
  → 1450ms → diagnosis

diagnosis
  → 用户点击“查看适合的解决方向” → directions

directions
  → 用户选择任一方向 → 360ms → products

products
  → 用户点击“再看一次” → analyzing
```

### 9.3 重置规则

每次发送新问题或点击“重新演示”时必须清空：

- 上一次补充回答。
- 两道追问的选择。
- 两个“其他”输入框。
- 方案方向选择。
- 分析阶段进度。

不得残留上一次问诊状态。

## 10. 分析阶段

### 10.1 顶部状态行

状态行包含：

- 34 × 34px 的浅蓝圆形助手标识。
- “牧客智语”。
- 胶囊状态标签。

状态标签依次显示：

1. 正在整理症状。
2. 正在分析原因。
3. 正在生成建议。
4. 分析完成。

### 10.2 进度条

- 轨道高度：3px。
- 背景：`#DFE9ED`。
- 进度色：`#216A8A`。
- 每阶段宽度约增加 33.34%。
- 宽度动画时长约 450–550ms。
- 下方显示“症状整理 / 原因分析 / 建议生成”。
- 已到达的文字变为主色，未到达保持浅灰。

分析结束时，旧内容向上轻移 8px 并淡出；追问卡片从下方 18px 淡入。

## 11. 关键追问

### 11.1 信息结构

追问卡片包含：

- 胶囊提示：“还需要确认 2 项信息”。
- 标题：“我先确认一下仔猪的阶段和粪便情况”。
- 说明：“请分别完成下面两项；没有合适选项时，可以自己补充。”
- 问题 1：这些仔猪大约多大？
- 问题 2：粪便更接近哪种情况？
- 回答进度。
- “完成并继续”按钮。

### 11.2 问题与选项

问题 1 选项：

- 刚断奶不久。
- 还在哺乳期。
- 其他。

问题 2 选项：

- 黄色稀便。
- 水样便。
- 便中带血。
- 其他。

### 11.3 强制交互约束

1. 点击任意选项只更新当前问题，不立即进入下一阶段。
2. 两道问题必须分别拥有有效答案。
3. 页面显示 `0/2`、`1/2` 或 `2/2 已回答`。
4. 两题未完成时，“完成并继续”禁用。
5. 两题完成后也不自动跳转，必须由用户主动点击“完成并继续”。
6. 用户可以在继续之前反复修改选择。
7. 选择“其他”后必须出现文本输入框。
8. “其他”输入为空时，该题不算有效答案。
9. 前端只能合并用户真实选择或输入，不得添加未回答的信息。

有效性判断：

```ts
const ready = Boolean(
  ageAnswer &&
  stoolAnswer &&
  (ageAnswer !== "其他" || ageOther.trim()) &&
  (stoolAnswer !== "其他" || stoolOther.trim())
);
```

### 11.4 选中态

- 默认：浅背景、蓝色文字、浅蓝边框。
- 选中：主蓝背景、白色文字、主蓝边框、轻微蓝色阴影。
- 按下：缩放到约 0.97。
- “其他”输入框以高度展开和透明度渐显出现。

## 12. 二次分析

用户完成追问后展示独立分析块：

- 左侧为 30 × 30px 环形加载动画。
- 右侧标题：“正在结合补充信息进一步判断”。
- 说明：“病例信息已更新，正在核对疾病特征与风险信号…”。
- 加载环持续旋转，单圈约 850ms。
- 本 Demo 停留约 1450ms 后进入辅助诊断。

若用户开启“减少动态效果”，应停止非必要动画，使用静态加载状态或缩短过渡。

## 13. 辅助诊断结果

### 13.1 展示结构

```text
已回答追问卡片
已确认病例信息（中文标签）
已完成本轮辅助分析
辅助诊断标题
可信度标签
支持判断
风险与局限提示
查看解决方向按钮
```

### 13.2 当前 Demo 文案

- 标题：“初步判断：更符合仔猪断奶后腹泻”。
- 可信度：“中等可信度”。
- 支持判断只允许使用用户已经提供的内容。
- 风险提示必须明确“这不是线上确诊”。

### 13.3 内容约束

- 不使用“确诊”“就是”“肯定是”等确定性措辞。
- 未收到用户饮水、体温、死亡数量等信息时，不得将其写入支持依据。
- “用户未描述某症状”只能表达为“目前没有收到相关描述”，不能表达为“确认没有”。
- 紧急风险必须高于产品推荐优先级。

### 13.4 顺序显现动画

诊断容器整体淡入，内部按约 100ms 间隔依次显示：

1. 完成提示。
2. 诊断标题。
3. 可信度。
4. 支持判断。
5. 风险提示。
6. 下一步按钮。

每项从下方 10–12px 移入，不能使用大幅弹跳。

风险提示允许执行一次克制的呼吸式阴影动画，持续约 1.1s，只执行一次。

## 14. 解决方向选择

### 14.1 进入方式

诊断结果底部提供全宽主按钮“查看适合的解决方向”。用户点击后，诊断依据继续保留，解决方向区块追加在下方。

### 14.2 当前方向数据

| 顺序 | 方向 | 说明 | 标签 |
| ---: | --- | --- | --- |
| 1 | 补液与电解质支持 | 优先稳定饮水、纠正脱水，适合当前阶段 | 优先建议 |
| 2 | 肠道微生态调理 | 帮助恢复肠道菌群与消化状态 | 温和支持 |
| 3 | 环境与饲喂管理 | 降低换料、温差和圈舍潮湿带来的应激 | 配套措施 |

这些内容在 Demo 中用于展示。正式产品中必须由节点 6 的固定规则表返回。

### 14.3 方向卡片

每一行包含：

- 圆形序号。
- 方向名称。
- 一行或两行说明。
- 右侧标签。
- 进入箭头。

默认第一项使用浅蓝推荐背景，但它仍然需要用户点击。不得因为默认突出而自动选中。

点击某项后：

1. 当前项进入选中态。
2. 保持约 360ms，确保用户看到反馈。
3. 再进入最终方案。

## 15. 产品与管理方案

### 15.1 当前 Demo 结构

```text
方案已生成
方案标题
Demo 与非处方说明
产品品类示例
配套管理建议
流程完成提示
再看一次
```

### 15.2 产品卡片字段

- 推荐层级，例如“首选”。
- 产品或品类名称。
- 适用目的。
- 使用方式。
- 观察重点。

当前展示“仔猪口服补液盐（示例品类）”，必须保留“示例品类”或等价的 Demo 标记。

### 15.3 正式接入要求

未来接入节点 7 时：

- 产品必须来自正式产品知识库。
- 前端不得自行生成产品名称、厂家、剂量或购买渠道。
- 没有匹配产品时显示无匹配状态，不能使用占位产品冒充结果。
- 管理型方向不得强制套用产品字段。
- 混合结果先展示产品，再展示配套管理建议。

## 16. 历史对话抽屉

### 16.1 布局

- 从左侧滑入。
- 宽度：视口的 82%。
- 覆盖完整应用高度。
- 右侧显示半透明遮罩。
- 动画时长约 240ms。
- 打开时锁定背景交互。

### 16.2 内容

- 标题“问诊记录”。
- 搜索框。
- 按“今天”“近 7 天”分组的历史列表。
- 每条记录右侧三点菜单。
- 底部设置入口。
- 底部真实昵称与个人信息入口。

三点菜单通过手机范围内的 Bottom Sheet 展示：重命名、置顶、删除。

删除是危险操作，正式版本必须二次确认。

## 17. 设置页与个人信息页

### 17.1 设置页

首版包含：

- 回答详细程度。
- 字体大小。
- 隐私政策与用户协议。
- 意见反馈。
- 当前版本。
- 清空历史记录。

不提供语音自动播报开关。

### 17.2 个人信息页

首版包含：

- 头像。
- 昵称。
- 脱敏手机号。
- 微信绑定状态。
- 退出登录。

不增加猪场、栏舍、存栏、免疫计划或复杂养殖档案。

## 18. 动画规范

### 18.1 动画原则

- 动画用于解释状态变化，不用于装饰。
- 单次位移通常不超过 20px。
- 禁止连续弹跳、强烈缩放和长时间闪烁。
- 核心按钮点击后必须立即显示视觉反馈。
- 自动流程必须让用户知道系统当前在处理什么。

### 18.2 动画清单

| 场景 | 动画 | 时长 |
| --- | --- | ---: |
| 用户消息出现 | 透明度 0→1，Y 8→0 | 约 200–300ms |
| 分析进度 | 宽度分三段增长 | 每段约 550ms |
| 分析阶段停留 | 自动推进 | 每段 780ms |
| 分析到追问 | 旧内容 Y 0→-8 并淡出；新内容 Y 18→0 | 约 200–300ms |
| “其他”输入出现 | 高度 0→auto，透明度 0→1 | 约 200ms |
| 二次分析加载环 | 360°线性旋转 | 850ms/圈 |
| 二次分析等待 | 自动停留 | 1450ms |
| 诊断内容 | 子元素依次淡入和上移 | 间隔约 100ms |
| 风险提示 | 一次轻微阴影呼吸 | 1100ms |
| 选择方向 | 立即显示选中态，随后进入方案 | 360ms |
| 历史抽屉 | X -105%→0 | 240ms |
| 选项按下 | scale 1→0.97→1 | 约 140ms |

### 18.3 减少动态效果

当 `prefers-reduced-motion: reduce` 生效时：

- 关闭风险提示呼吸动画。
- 关闭抽屉和输入区的大部分过渡。
- 分析状态可以保留文字变化，但避免持续旋转或明显位移。
- 不得因为关闭动画而跳过任何业务状态。

## 19. 键盘与安全区域

1. 所有文本输入必须使用键盘感知输入组件。
2. 输入框聚焦时显示模拟键盘。
3. 键盘出现后，底部输入区应位于键盘正上方。
4. 打开抽屉、Bottom Sheet 或切换页面前先关闭键盘。
5. 输入框失焦时关闭键盘，除非焦点直接移向另一个输入框。
6. iPhone 内容需要避开状态栏和 Home Indicator。
7. Android 关闭键盘时为系统导航栏保留空间。
8. 正文必须可以滚动到固定输入框上方，最后一段内容不能被遮挡。

## 20. 响应式规则

### 20.1 手机端

- 设计基准约为 390 × 844px。
- 页面保持单列。
- 追问选项允许自动换行。
- 长内容在正文区纵向滚动。
- 不允许页面横向滚动。

### 20.2 桌面展示

当前 Demo 以手机模拟器方式在桌面展示。未来正式 Web 版可将聊天主栏最大宽度限制在 720–800px，但必须保持相同的信息顺序、语义色彩和交互状态。

## 21. 无障碍规范

- 所有图标按钮必须具有可访问名称。
- 不得仅依靠颜色表达选中、禁用、风险或完成状态。
- 禁用按钮必须同时具有 `disabled` 属性。
- 选项按钮应暴露选中状态，正式版本建议使用 `aria-pressed`。
- 抽屉打开后应限制焦点在抽屉内，关闭后恢复到触发按钮。
- 文字放大后不能截断关键按钮文案。
- 风险提示的对比度必须达到正文可读标准。

## 22. 推荐数据接口形状

其他 AI 在扩展此 Demo 时，应尽量使用数据驱动组件，不把某个疾病直接写死在 JSX 结构中。

```ts
interface FollowUpQuestion {
  id: string;
  question: string;
  options: string[];
  allowOther: boolean;
  required: boolean;
}

interface DiagnosisViewModel {
  title: string;
  confidence: "低" | "中等" | "较高";
  evidence: string[];
  missingInformation: string[];
  caution: string;
  emergency: boolean;
}

interface SolutionDirection {
  id: string;
  title: string;
  description: string;
  tag?: string;
  directionType: "product" | "management" | "mixed";
}

interface ProductResult {
  resultMode: "product" | "management" | "mixed" | "no_match";
  products: Array<{
    name: string;
    manufacturer?: string;
    purpose: string;
    usage: string;
    observation: string;
    sourceId: string;
  }>;
  managementAdvice: string[];
  disclaimer: string;
}
```

## 23. AI 实施约束

任何接手此项目的 AI 必须遵循：

1. 先阅读本文件和项目内 `AGENTS.md`。
2. 只在应用页面文件和对应样式文件中实现业务 UI，除非明确要求修改手机运行框架。
3. 不删除设备切换、状态栏、键盘、安全区和滚动运行时。
4. 不用 Emoji 替代正式图标。
5. 不使用 CSS 绘制品牌头像或业务插画。
6. 不改变“雾蓝牧场”主题，除非用户明确要求重新设计。
7. 不把连续诊断内容拆成大量小卡片。
8. 不让追问选项点击后立即自动推进。
9. 不替用户补全未回答的信息。
10. 不把 Demo 产品当成正式知识库产品。
11. 不使用确定性医疗措辞。
12. 不在高风险病例后继续普通产品推荐。
13. 修改后必须运行运行时完整性检查和生产构建。

## 24. 验收清单

### 24.1 视觉

- [ ] 背景、主色、边框和风险色符合令牌。
- [ ] 所有字体使用统一中文字体栈。
- [ ] 顶部栏始终位于安全区下方。
- [ ] AI 内容保持连续阅读结构。
- [ ] 卡片阴影克制，没有电商化视觉。
- [ ] 图标风格统一，无 Emoji。

### 24.2 追问

- [ ] 两道问题可以分别选择。
- [ ] 点击第一题不会推进流程。
- [ ] 未完成两题时继续按钮禁用。
- [ ] 两题完成后仍需用户主动点击继续。
- [ ] 两题都提供“其他”。
- [ ] “其他”为空时继续按钮禁用。
- [ ] 用户可以修改已选答案。
- [ ] 最终补充信息只包含用户真实回答。

### 24.3 流程

- [ ] 首页示例问题可以启动问诊。
- [ ] 三段分析状态按顺序显示。
- [ ] 追问完成后显示二次分析。
- [ ] 辅助诊断包含可信度和风险提示。
- [ ] 解决方向必须由用户主动打开。
- [ ] 方向必须由用户主动选择。
- [ ] 选择后显示视觉确认再进入方案。
- [ ] 最终产品明确标注为 Demo 示例。
- [ ] “重新演示”和“再看一次”可以完整重置状态。

### 24.4 工程

- [ ] `npm run check:runtime` 通过。
- [ ] `npm run build` 通过。
- [ ] `npm run test:sites` 通过。
- [ ] 页面不存在横向溢出。
- [ ] 固定输入区不遮挡最终内容。
- [ ] iPhone 和 Pixel 10 均可完成核心流程。

## 25. 文档优先级

当实现与资料冲突时，按以下顺序处理：

1. 用户在当前任务中的明确要求。
2. 本 `design.md` 中的交互与视觉规范。
3. 项目 `AGENTS.md` 中的运行框架约束。
4. 产品 PRD 与工作流文档中的业务规则。
5. 当前代码中的历史实现。

如果代码行为与本文件冲突，应修正代码，而不是降低本文约束。
